# ivert upgrade

Upgrade IVERT to the latest available version.

```
ivert upgrade
```

This checks for a newer version of IVERT and installs it using `pip`. No arguments or options are needed.

If IVERT is installed in a virtual environment, the upgrade applies to that environment. Re-activate the environment after upgrading if you notice the `ivert` command still reports the old version.
