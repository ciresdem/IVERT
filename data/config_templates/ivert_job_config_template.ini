[DEFAULT]
job_username = [USERNAME]
job_email = [EMAIL_ADDRESS]

# The following variables will be filled in by
job_id = [JOB_ID]
job_ingest_bucket = [INGEST_BUCKET]
job_ingest_prefix = [INGEST_PREFIX]
job_export_bucket = [EXPORT_BUCKET]
job_export_prefix = [EXPORT_PREFIX]

# The command being sent. At this time, supported commands are: "validate", "update", or "import"
job_command = [JOB_COMMAND]
# The rest of the command-line prompts that the user has entered.
job_params = [PARAMS_STRING]